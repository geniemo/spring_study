spring:
# 아무것도 안적으면 스프링부트가 알아서 메모리 기반으로 바꿔준다.
#  datasource:
#    url: jdbc:h2:mem:test # 메모리 기반
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.sql: debug
    org.hibernate.type: trace